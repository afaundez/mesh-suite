#include "headers/PreProcessFactory.h"

PreProcess* PreProcessFactory::build(Constant::PreProcessType type){
    PreProcess* ret;
    switch(type){
    default:
        break;
    }

    return ret;
}

